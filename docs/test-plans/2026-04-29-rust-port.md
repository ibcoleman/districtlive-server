# Human Test Plan — DistrictLive Rust Port

Generated from test-analyst coverage pass against `test-requirements.md`.
Automated coverage: **28 / 28 criteria** (100%).

---

## Prerequisites

- `just dev` running: kind cluster, Postgres, and Rust server all healthy
- `just check` passes (no fmt or clippy warnings)
- `just test` passes (53 tests: api + properties + connector_tests)
- `just test-integration` passes against a live Postgres (all integration_db.rs tests)
- `cd frontend && pnpm build` exits 0 and produces `dist/index.html`, `dist/src/pages/ingestion/index.html`, `dist/src/pages/featured/index.html`
- Admin credentials in scope: username `admin`, password `changeme` (from `just dev` defaults)

---

## Phase 1: Smoke Test Public API

| Step | Action | Expected |
|------|--------|----------|
| 1.1 | `curl -s http://localhost:8080/api/events \| jq .` | HTTP 200; JSON with `total` (integer), `page`, `per_page`, `items` (array) at top level |
| 1.2 | `curl -s "http://localhost:8080/api/events?date_from=not-a-date"` | HTTP 400 |
| 1.3 | `curl -s "http://localhost:8080/api/events/<unknown-uuid>" \| jq .` | HTTP 404 with JSON error body |
| 1.4 | `curl -s http://localhost:8080/api/venues \| jq .` | HTTP 200; array of venue objects each containing `id`, `name`, `slug`, `neighborhood` |
| 1.5 | `curl -s "http://localhost:8080/api/venues?neighborhood=U+Street" \| jq '.[].neighborhood'` | All returned values equal `"U Street"` (or the neighbourhood stored in seed data) |
| 1.6 | `curl -s "http://localhost:8080/api/artists?local=true" \| jq '.[].is_local'` | All returned values equal `true` |
| 1.7 | `curl -s http://localhost:8080/api/featured \| jq .` | HTTP 200; object contains `id`, `blurb`, `event` (with `title`, `venue`, `artists`) |

---

## Phase 2: Smoke Test Admin API

| Step | Action | Expected |
|------|--------|----------|
| 2.1 | `curl -s http://localhost:8080/api/admin/sources` | HTTP 401 (no Authorization header) |
| 2.2 | `curl -s -u wrong:creds http://localhost:8080/api/admin/sources` | HTTP 401 |
| 2.3 | `curl -s -u admin:changeme http://localhost:8080/api/admin/sources \| jq .` | HTTP 200; array where each element has `id`, `name`, `source_type`, `healthy`, `consecutive_failures`, `last_success_at` |
| 2.4 | Pick a `<source_id>` from step 2.3. `curl -s -u admin:changeme "http://localhost:8080/api/admin/sources/<source_id>/history" \| jq '.[].started_at'` | Array of timestamps in descending order |
| 2.5 | `curl -s -u admin:changeme -X POST http://localhost:8080/api/admin/ingest/trigger` | HTTP 400 with body containing `"ingestion is disabled"` (default config has `INGESTION_ENABLED=false`) |

---

## Phase 3: Ingestion Trigger (requires `INGESTION_ENABLED=true`)

| Step | Action | Expected |
|------|--------|----------|
| 3.1 | Stop server; set `INGESTION_ENABLED=true`; restart | Server starts without error; scheduler log messages appear |
| 3.2 | `curl -s -u admin:changeme -X POST http://localhost:8080/api/admin/ingest/trigger \| jq .` | HTTP 200; JSON body contains integer fields `events_fetched`, `events_created`, `events_updated`, `events_deduplicated` |
| 3.3 | `just psql -c "SELECT COUNT(*) FROM ingestion_runs WHERE status = 'SUCCESS';"` | Count >= 1 |
| 3.4 | `just psql -c "SELECT COUNT(*) FROM events;"` | Count > 0 (events were ingested) |
| 3.5 | Pick a source `<id>`. `curl -s -u admin:changeme -X POST "http://localhost:8080/api/admin/ingest/trigger/<id>"` | HTTP 200; JSON stats for that single source |
| 3.6 | Run the trigger a second time for the same source. Check `jq '.events_created'` | `events_created` = 0; `events_updated` >= 0 (slug upsert correctly deduplicates) |

---

## Phase 4: Live MusicBrainz Enrichment (AC4.1 human verification)

Purpose: Verify the enrichment pipeline calls the live MusicBrainz API, respects the rate limit, and writes canonical name, MBID, and tags back to the artist row.

| Step | Action | Expected |
|------|--------|----------|
| 4.1 | Set `ENRICHMENT_ENABLED=true`; restart server | Log shows `enrichment scheduler started` |
| 4.2 | Ingest events that include `"Fugazi"` as an artist name (use trigger or seed) | `SELECT name, enrichment_status FROM artists WHERE name = 'Fugazi'` returns `enrichment_status = 'PENDING'` |
| 4.3 | Set `ENRICHMENT_CRON="* * * * *"` and restart to fire in ~1 min; wait for tick | Log shows `claiming batch` then `batch completed` |
| 4.4 | `just psql -c "SELECT name, canonical_name, musicbrainz_id, enrichment_status, array_length(mb_tags, 1) FROM artists WHERE name = 'Fugazi';"` | `enrichment_status = 'DONE'`, `musicbrainz_id` non-null UUID, `array_length(mb_tags, 1) > 0`, `canonical_name` non-null |
| 4.5 | `just psql -c "SELECT name, enrichment_status FROM artists WHERE enrichment_status = 'FAILED';"` | Zero rows (or only genuinely unknown artists) |

---

## Phase 5: Frontend — Ingestion Monitor (AC6.2, AC6.3)

| Step | Action | Expected |
|------|--------|----------|
| 5.1 | Open `http://localhost:8080/admin/ingestion` in a browser | Login prompt appears |
| 5.2 | Enter username `admin`, password `changeme`; click Login | Page re-renders showing source cards grid |
| 5.3 | Count displayed source cards against `just psql -c "SELECT COUNT(*) FROM sources;"` | Card count matches row count |
| 5.4 | Inspect a healthy source card | Card shows a green "healthy" indicator and a last-success timestamp |
| 5.5 | `just psql -c "UPDATE sources SET consecutive_failures = 5, healthy = false WHERE name = 'dc9';"` then hard-refresh | The dc9 card shows an unhealthy indicator with failure count |
| 5.6 | Open DevTools → Network; click "Trigger" on any source card | Button text changes; `POST /api/admin/ingest/trigger/<id>` request appears in Network tab with status 200 |
| 5.7 | After trigger completes | Grid re-renders; button becomes clickable again |
| 5.8 | `just psql -c "SELECT COUNT(*) FROM ingestion_runs WHERE status IN ('SUCCESS','FAILED') ORDER BY started_at DESC LIMIT 5;"` | A new row exists for the triggered source |

---

## Phase 6: Frontend — Featured Events Admin (AC6.4)

| Step | Action | Expected |
|------|--------|----------|
| 6.1 | `just psql -c "SELECT id FROM events LIMIT 1;"` | One UUID printed |
| 6.2 | Open `http://localhost:8080/admin/featured`; log in | Page renders with a "Create Featured" form and a "History" section |
| 6.3 | Note existing history entries | Entries visible with event title, blurb, and created_by |
| 6.4 | Paste UUID from 6.1 into Event ID field; leave Blurb blank; click "Create" | Inline error appears: blurb cannot be blank; form does not submit |
| 6.5 | Fill in a non-empty blurb; click "Create" | Page re-renders; new entry appears at top of History with correct event title, blurb, and `created_by = "admin"` |
| 6.6 | `just psql -c "SELECT event_id, blurb, created_by FROM featured_events ORDER BY created_at DESC LIMIT 1;"` | Row matches UUID from 6.1, blurb from 6.5, created_by = `"admin"` |

---

## End-to-End: Full Ingest-to-Display Pipeline

1. With `INGESTION_ENABLED=true`, call `POST /api/admin/ingest/trigger`. Record `events_created` (should be > 0 on a fresh DB).
2. `GET /api/events?per_page=5` — confirm `items[0]` contains `title`, `slug`, `venue.name`, `artists[]`, `start_time`, and `status = "Active"`.
3. `GET /api/events/<id-from-step-2>` — confirm `venue` is a full object (not null), `artists` is a non-empty array, and `related_events` is present (may be empty).
4. With `ENRICHMENT_ENABLED=true` and cron set to fire soon, wait one tick. `GET /api/artists?local=false` — confirm at least one artist has a non-null `canonical_name` or `musicbrainz_id`.
5. `GET /api/featured` — confirm `event` is hydrated (has `title`, `slug`, `venue`) and `blurb` is non-empty.

---

## Criteria Requiring Human Verification

| Criterion | Why Manual | Steps |
|-----------|------------|-------|
| AC4.1 (live enrichment) | Live MusicBrainz HTTP; non-deterministic response | Phase 4 |
| AC6.1 (frontend build) | Build exit status; confirm HTML entry points present | Prerequisites |
| AC6.2 (source health cards render) | Browser DOM rendering; no JS test infrastructure in scope | Phase 5, steps 5.2–5.5 |
| AC6.3 (trigger button calls API + refreshes) | Browser click → fetch → re-render; requires live server | Phase 5, steps 5.6–5.8 |
| AC6.4 (featured admin form and history) | Form submission and history list rendering in browser | Phase 6 |

---

## Traceability

| AC | Automated Test | Manual Steps |
|----|----------------|--------------|
| AC1.1 | `integration_db::ac1_1_migrations_apply_cleanly` | — |
| AC1.2 | `integration_db::ac1_2_expected_tables_exist` | — |
| AC1.3 | `integration_db::ac1_3_seed_data_present` | — |
| AC1.4 | `integration_db::ac1_4_event_upsert_updates_on_slug_conflict` | — |
| AC2.1 | `integration_db::ac2_1_list_events_returns_paginated_shape` | Phase 1, step 1.1 |
| AC2.2 | `integration_db::ac2_2_event_filters_narrow_results` | Phase 1, step 1.5 |
| AC2.3 | `integration_db::ac2_3_event_detail_includes_venue_artists_related` | E2E step 3 |
| AC2.4 | `integration_db::ac2_4_venues_neighborhood_filter` | Phase 1, step 1.5 |
| AC2.5 | `integration_db::ac2_5_artists_local_filter` | Phase 1, step 1.6 |
| AC2.6 | `integration_db::ac2_6_featured_returns_most_recent` | Phase 1, step 1.7 |
| AC2.7 | `api::get_event_unknown_id_returns_404` | Phase 1, step 1.3 |
| AC2.8 | `api::list_events_bad_date_returns_400` | Phase 1, step 1.2 |
| AC3.1 | `connector_tests::*` (11 tests) | — |
| AC3.2 | `properties::slug_is_non_empty_for_valid_input`, `slug_is_url_safe` | — |
| AC3.3 | `deduplication::tests::deduplication_merges_matching_slugs` | — |
| AC3.4 | `integration_db::ac3_4_admin_trigger_returns_stats` (partial) | Phase 3, step 3.2 |
| AC3.5 | `integration_db::ac3_5_ingestion_run_lifecycle` | Phase 3, steps 3.3–3.4 |
| AC3.6 | `integration_db::ac3_6_repeat_connector_run_updates_existing_event` | Phase 3, step 3.6 |
| AC3.7 | `normalization::tests::placeholder_titles_are_filtered` | — |
| AC3.8 | `api::admin_ingest_trigger_when_disabled_returns_400` | Phase 2, step 2.5 |
| AC4.1 (unit) | `musicbrainz::tests::parse_response_returns_result_*` | — |
| AC4.1 (integration) | `integration_db::ac4_1_pending_artist_transitions_to_done` | Phase 4 |
| AC4.2 | `integration_db::artist_marked_skipped_after_max_attempts` | — |
| AC4.3 | `integration_db::startup_reset_clears_in_progress` | — |
| AC4.4 | `integration_db::transient_error_marks_failed_not_skipped` | — |
| AC4.5 | `spotify::tests::new_returns_none_when_spotify_disabled` | — |
| AC5.1 | `api::admin_valid_credentials_returns_200` | Phase 2, step 2.3 |
| AC5.2 | `integration_db::ac5_2_source_history_ordered_desc` | Phase 2, step 2.4 |
| AC5.3 | `integration_db::ac5_3_create_featured_returns_created` | Phase 6, step 6.5 |
| AC5.4 | `api::admin_without_auth_returns_401` | Phase 2, step 2.1 |
| AC5.5 | `api::admin_wrong_credentials_returns_401` | Phase 2, step 2.2 |
| AC5.6 | `api::create_featured_blank_blurb_returns_400` | Phase 6, step 6.4 |
| AC5.7 | `api::create_featured_unknown_event_returns_404` | Phase 6, step 6.5 (bad UUID) |
| AC6.1 | `pnpm build` exits 0 | Prerequisites |
| AC6.2 | — | Phase 5, steps 5.2–5.5 |
| AC6.3 | — | Phase 5, steps 5.6–5.8 |
| AC6.4 | — | Phase 6 |
| AC7.1 | `connector_tests::*` (11 tests) | — |
| AC7.2 | `properties::slug_*`, `normalization_preserves_start_time` | — |
| AC7.3 | `properties::deduplication_is_idempotent` | — |
| AC7.4 | `just check` (fmt + clippy) | — |
